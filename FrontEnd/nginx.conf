# server {
#   listen 80;
#   server_name _;
#   root /usr/share/nginx/html;
#   index index.html;
#   large_client_header_buffers 8 32k;
#   client_max_body_size 10m;
#   location /api/ {
#     proxy_pass http://api:3000/api/;
#     proxy_set_header Host              $host;
#     proxy_set_header X-Real-IP         $remote_addr;
#     proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Cookie "";
#     proxy_buffer_size       16k;
#     proxy_buffers           8 16k;
#     proxy_busy_buffers_size 24k;
#   }

#   location = /api {
#     proxy_pass http://api:3000/api;
#     proxy_set_header Host              $host;
#     proxy_set_header X-Real-IP         $remote_addr;
#     proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Cookie "";
#   }

#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }

server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # (opcional) defensivo
  large_client_header_buffers 8 32k;
  client_max_body_size 10m;

  # ===== API =====
  # Da prioridad fuerte a /api/ para que NO caiga en el fallback del SPA
  location ^~ /api/ {
    # Mantén la URI completa tal cual (incluye /api/…)
    proxy_pass http://api:3000;

    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Si tu API no usa cookies, evita el 400 por cookies grandes
    proxy_set_header Cookie "";

    proxy_buffer_size       16k;
    proxy_buffers           8 16k;
    proxy_busy_buffers_size 24k;
  }

  # Exacto /api (sin slash)
  location = /api {
    proxy_pass http://api:3000;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Cookie "";
  }

  # ===== SPA =====
  location / {
    try_files $uri $uri/ /index.html;
  }
}
